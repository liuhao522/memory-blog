---
name: daily-2026-07-01
description: 7/1工作日志 — 统计大屏Tab2/Tab3/Tab4多项修复与功能实现
metadata:
  type: project
  tech: [Java Spring Boot, Vue 3, ECharts, MySQL, MyBatis-Plus]
  date: 2026-07-01
---

# 2026-07-01 工作日志

## 统计大屏

### Tab3 客户洞察 — 溯源 500 修复
- **问题**: admin 账号点击"溯源该时段现场原始录音声轨"返回 500
- **根因**: `onDrilldown` 传参缺少 `customerId`/`recordTime`；admin 跳过租户隔离导致 SQL 无过滤条件
- **修复**: 补全参数，`startTime/endTime` 改为 `recordTime` 数组格式匹配 `analysisDetail` 页

### Tab2 服务质量 — 智能洞察报告对接
- 新增后端端点 `GET /qualityStatistics/intelligence-report`
- 三块数据：TOP10词频+趋势（双周期对比）、TOP5优质客户（高意向低风险）、TOP5流失客户（高风险）
- 前端 `StatsIntelligenceReport.vue` 删除全量 Mock，接真实 API
- 趋势判定按 period 自动对比上一周期（日→昨天，月→上月，年→去年）

### Tab2 客户关注点/抗拒点趋势图改柱状图
- 堆叠面积图 → 平铺柱状图
- 修复横坐标：SQL `DATE_FORMAT` 动态注入（`%Y-%m-%d` / `%Y-%m` / `%Y`）
- 按日/月/年仅展示近 3 个周期

### Tab3 客户360画像 — 首次加载优化
- 遍历客户列表直到找到当前时间范围内有沟通记录的客户

### Tab4 人员效能
- 删除质检模型"全部"按钮，首次加载自动选第一个模型
- 能力诊断雷达图 SQL 加 `LIMIT 3`，只取最近 3 条记录
- 成长曲线按日显示最近 10 条原始记录 + 日期横轴
- 优秀线改为 `td_ai_quality_model.threshold` 动态读取

## 代码审查
- `intelligenceReport` 服务层加固：null 参数校验、三态趋势(up/down/stable)、`"null"` 字面量过滤
